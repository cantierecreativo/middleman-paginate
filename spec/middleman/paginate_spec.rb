require 'spec_helper'

describe Middleman::Paginate do
  it 'has a version number' do
    expect(Middleman::Paginate::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
